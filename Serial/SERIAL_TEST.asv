%% CONNECT
[PORT, tmr] = serial_setup_open('COM3',115200);
if(PORT~=0)
   disp('CONNECTED'); 
end

%% SHOULD BE RUNNING IN INTERRUPT MODE

protocol_send_command(PORT,101); %Trigger the motion

%% CLOSE
serial_close(PORT);
