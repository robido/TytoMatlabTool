function serial_ISR_TX(hObject, event, serConn)
%SERIAL_ISR_TX % Called when data is done sending
%disp('TX_ISR_TRIGGERED');
%if( serConn.BytesToOutput()==0)
%    if strcmp('idle',serConn.TransferStatus())
        
%    end
%end

